<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Video Duration at Different Speeds</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <h1>Total Duration of the playlist :</h1>
    <p><%=totalDuration %></p>
    <h1>Average Duration :</h1>
    <p><%=averageDuration %></p>
    <h1 class="text-center">Video Duration at Different Playback Speeds</h1>

    <table class="table table-bordered table-striped mt-4">
      <thead>
        <tr>
          <th>Playback Speed</th>
          <th>Duration</th>
        </tr>
      </thead>
      <tbody>
        <% for (let speed in speedDurations) { %>
          <tr>
            <td><%= speed %></td>
            <td><%= speedDurations[speed] %></td>
          </tr>
        <% } %>
      </tbody>
    </table>

    <div class="text-center mt-4">
      <a href="/" class="btn btn-primary">Go Back</a>
    </div>
  </div>
</body>
</html>
